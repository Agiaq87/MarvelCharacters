package it.giaquinto.marvelcharacters.ui.widget

/*@Composable
fun alert(context: Context, title: String?, description: String) =
    Box(
        contentAlignment = Alignment.Center,
        modifier = Modifier
            .padding(ThemeSpacing.defaultPadding)
            //.border(ThemeBorder.alertDialogBorder.width)
    ){
        Column {
            Row {
                Text(
                    text = title ?: context.getString(R.string.default_alert_dialog_title),
                    color = ThemeColor.primaryWhite,
                    modifier = Modifier
                        .background(ThemeColor.logoRed)
                        .padding()

                )
            }
        }
    }*/


