package it.giaquinto.marvelcharacters.ui.theme

/*
@Composable
fun MarvelCharactersTheme (content: @Composable () -> Unit) {
    CompositionLocalProvider(
        LocalElevations provides Elevation(cardElevation = 2.dp)
    ) {
        MaterialTheme(
            colors = lightColors(
                primary = primaryWhite,
                secondary = backgroundTextRed,
                onPrimary =
            )
        )
    }
}*/
